# humming_code

Hamming code is a set of error-correction codes that can be used to detect and correct the errors that can occur when the data is moved or stored from the sender to the receiver. It is technique developed by R.W. Hamming for error correction.


In the application, the text to be transmitted is entered and the binary representation of the text is presented. A random error is added to some bits of the message and these bits are found and corrected with the humming method.
